export const emailPattern = new RegExp('^[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,4}$');
export const namePattern = new RegExp('([a-zA-Z]+) ([a-zA-Z]+)');
export const notOnlySpacesPattern = new RegExp('^[a-zA-Z0-9]+$');
